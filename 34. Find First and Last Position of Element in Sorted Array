class Solution:
    def searchRange(self, nums: List[int], target: int) -> List[int]:
        # low = 0
        # high = len(nums)-1 
        
        # while low <= high
        #    mid = (low + high ) // 2
        #  if nums[mid] = target
        #   return mid
        #  elif nums[mid] < target
        #    low = mid + 1
        #  else:
        #     high = mid +1
        #     if is_searching_low:
        #         high = mid -1
        #     else: left = mid +1       


        n = len(nums)
        first =-1
        last =-1
        for i in range (n):
            if (nums[i] == target):
              if (first==-1):
                first = i
              last = i
        return [first,last]        
